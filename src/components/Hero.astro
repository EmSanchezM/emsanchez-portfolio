<section class="hero" id="hero">
  <div class="container">
    <article class="grid hero__wrapper">
      <main class="hero__content">
        <h1 class="hero__title">
          Hi, I'm Elvin, <br /> Software engineer.
        </h1>
        <p class="hero__description">
          Software engineer with +4 years of experience in web and mobile development.
          Proficient in Java, JavaScript, Python and frameworks like React, Vue, Next.js, Nuxt.js, Nest.js, Spring Boot.
          Experienced in database design, API integration, and working with cross-functional teams.
        </p>
        <div class="hero__info">
          <div class="hero__info-wrapper">
            <span class="hero__info-number">+4</span>
            <h2 class="hero__info-title">Year<br />Experience</h2>
          </div>
          <div class="hero__info-wrapper">
            <p class="hero__info-number">25+</p>
            <h2 class="hero__info-title">Projects<br />Completed</h2>
          </div>
        </div>
      </main>
      <img
        src="/images/em-sanchez.webp"
        srcset="/images/em-sanchez-250.webp 250w,
          /images/em-sanchez-475.webp 475w"
        sizes="(max-width:968) 250px,475px"
        alt="Fotografía de Elvin Sánchez"
        class="hero__img"
        fetchpriority="high"
        decoding="sync"
        width="475px"
        height="475px"
      />
    </article>
  </div>
</section>

<style>
  .hero__wrapper {
    min-height: calc(100vh - var(--height-header));
    padding: 5rem 0;
  }

  .hero__content {
    text-align: center;
  }

  .hero__title {
    font-size: var(--fs-biggest);
    margin-bottom: 5rem;
    position: relative;
  }

  .hero__title::after {
    position: absolute;
    content: "";
    background-color: var(--primary-color);
    width: 10rem;
    height: 0.5rem;
    bottom: -2rem;
    left: 50%;
    transform: translateX(-50%);
  }

  .hero__description {
    margin-bottom: 5rem;
    text-wrap: balance;
    max-width: 65ch;
    line-height: 1.6;
  }

  .hero__info {
    border-top: 1px solid var(--border-color);
    padding-top: 2rem;
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 5rem;
  }

  .hero__info-number {
    font-size: var(--fs-xxl);
    font-weight: var(--fw-bold);
    color: var(--white);
    margin-bottom: 1rem;
  }

  .hero__info-title {
    font-size: var(--fs-sm);
    font-weight: var(--fw-regular);
    color: var(--text-color);
  }

  .hero__img {
    border-radius: 50%;
    box-shadow: 0 0 20px rgba(0, 0, 0, 0.2);
    object-fit: cover;
  }

  @media screen and (max-width: 968px) {
    .hero__wrapper {
      padding: 3rem 0;
      gap: 2rem;
    }

    .hero__img {
      width: 250px;
      height: 250px;
      margin: 0 auto;
    }

    .hero__content {
      padding: 0 1rem;
    }

    .hero__title {
      font-size: var(--fs-xl);
      margin-bottom: 3rem;
    }

    .hero__description {
      margin-bottom: 3rem;
    }
  }

  @media screen and (min-width: 768px) {
    .hero__wrapper {
      grid-template-columns: repeat(2, 1fr);
      align-items: center;
    }

    .hero__content {
      text-align: initial;
    }

    .hero__title::after {
      transform: none;
      left: 0;
    }

    .hero__info {
      justify-content: initial;
    }

    .hero__img {
      justify-self: flex-end;
    }
  }
</style>
